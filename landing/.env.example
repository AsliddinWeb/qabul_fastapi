# Landing-only env (when running `npm run dev` outside docker).
# In Docker mode, env vars from root .env are passed via docker-compose.

NUXT_PUBLIC_APP_URL=/app
NUXT_PUBLIC_API_BASE_URL=/api/v1
NUXT_PUBLIC_SITE_URL=https://qabul.xiuedu.uz
